<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="mapper.deli">
		<resultMap id="deliveryResult" type="deliveryVO">
			<result property="d_id" column="d_id"/>
			<result property="o_id" column="o_id"/>
			<result property="d_status" column="d_status"/>
			<result property="d_company" column="d_company"/>
			<result property="d_transport_num" column="d_transport_num"/>
			<result property="d_shipped_date" column="d_shipped_date"/>
			<result property="d_delivery_date" column="d_delivery_date"/>
		</resultMap>
		<insert id="insertNewdelivery" parameterType="deliveryVO">
			insert into delivery (o_id, d_transport_num, d_shipped_date, d_delivery_date)
			values(#{o_id}, #{d_transport_num}, #{d_shipped_date}, #{d_delivery_date})
		</insert>
		<update id="updatedelivery" parameterType="int">
			update delivery set d_status = 2 where o_id = #{o_id}
		</update>
	</mapper>